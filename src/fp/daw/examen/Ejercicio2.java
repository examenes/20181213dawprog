package fp.daw.examen;

public class Ejercicio2 {

	/* 
	 * 2 puntos
	 * 
	 * Define, después de este comentario, un método llamado 'busquedaBinaria' que declare dos
	 * parámetros formales: un vector de números enteros y un número entero. Asuminedo que el
	 * vector que se recibe estará ordenado, el método deberá encontrar mediante una búsqueda 
	 * binaria el índice de la posición en la que se encuentra almacenado el número dentro del
	 * vector, o -1 si éste no se encuentra almacenado en el vector. 
	 *  
	 */
	
	public static ...
	
	
	/*
	 * 1 punto
	 * 
	 * Escribe en el método main las sentencias necesarias para poner a prueba el método
	 * 'busquedaBinaria'.
	 * 
	 * En primer lugar creamos un vector de números enteros de un tamaño aleatorio entre
	 * 100 y 200 y lo llenamos con números aleatorios comprendidos entre 0 e Integer.MAX_VALUE - 1,
	 * sin que se repita ningún valor.
	 * 
	 * A continuación, generamos números aleatorios en el mismo intervalo para comprobar 
	 * con el método 'busquedaBinaria' si se encuentran almacenados en el vector, proceso
	 * que se repetirá mientras el valor generado no se encuentre dentro del vector.
	 *  
	 * Finalmente, mostramos el vector con ayuda del método 'mostrarVector' del ejercicio 1,
	 * el valor buscado, la posición que ocupa dentro del vector y el número de búsquedas.
	 * 
	 */
	
	public static void main(String[] args) {
		
		
	}

}
